'use client';
import React from 'react'

const AddToChart = () => {
  return (
    <div>
        <button className='btn btn-primary'  onClick={() => console.log('Add to cart clicked')}>Add To Chart</button>
    </div>
  )
}

export default AddToChart